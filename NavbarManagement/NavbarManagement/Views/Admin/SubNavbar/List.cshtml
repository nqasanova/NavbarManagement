@model List<ListItemViewModel>

<div class="col-lg-12">
    <div class="row justify-content-end">
        <div class="">
            <a asp-route="admin-subnavbar-add" target="_blank" class="btn btn-secondary">Add New Sub Navbar</a>
        </div>
    </div>
</div>
<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Navbars</h4>
        </div><!--end card-header-->
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped mb-0">
                    <thead>
                        <tr>
                            <th>Subnavbar Name</th>
                            <th>Subnavbar URL</th>
                            <th>Subnavbar Order</th>
                            <th>Subnavbar's Navbar</th>
                            <th class="text-end">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var navbar in Model)
                        {
                            <tr>
                                <td>@navbar.Name</td>
                                <td>@navbar.URL</td>
                                <td>@navbar.Order</td>
                                <td>@navbar.Navbar</td>
                                <td class="text-end">
                                    <a asp-route="admin-subnavbar-update" asp-route-id="@navbar.Id"><i class="las la-pen text-secondary font-16"></i></a>
                                    <form method="POST" asp-route="admin-subnavbar-delete" asp-route-id="@navbar.Id" id="subnavbar-form-@navbar.Id"></form>
                                    <button type="submit" form="subnavbar-form-@navbar.Id">
                                        <i class="las la-trash-alt text-secondary font-16"></i>
                                    </button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table><!--end /table-->
            </div><!--end /tableresponsive-->
        </div><!--end card-body-->
    </div><!--end card-->
</div>